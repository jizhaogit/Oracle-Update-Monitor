# Oracle OCI/OIC Monitor — CORE dependencies
# Lightweight set used in the portable/green package.
# No torch / sentence-transformers — semantic search falls back to SQLite FTS.
# Install full AI features separately with: pip install -r requirements-full.txt

# HTTP & Parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
fake-useragent>=1.4.0

# Database
sqlalchemy>=2.0.23

# LangChain (core only — no heavy model dependencies)
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20

# Scheduler
APScheduler>=3.10.4

# API Backend
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.7

# Configuration
python-dotenv>=1.0.0
pydantic>=2.0.0

# UI
ttkthemes>=3.2.2
Pillow>=10.0.0
